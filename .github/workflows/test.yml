name: Test

on:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: Landing Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node --test 'tests/*.test.mjs'
